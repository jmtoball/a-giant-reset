import { Neuton, Staatliches } from 'next/font/google';

export const bodyFont = Neuton({
  subsets: ['latin'],
  weight: '300',
  style: 'normal',
});
export const capsFont = Staatliches({
  subsets: ['latin'],
  weight: '400',
  style: 'normal',
});
